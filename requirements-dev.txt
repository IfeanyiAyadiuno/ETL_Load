# Dev / CI — install test dependencies
#   pip install -r requirements-dev.txt
#
# Windows (Command Prompt or PowerShell), from the project folder (e.g. I:\ETL_Load):
#   py -m pip install -r requirements-dev.txt
#   py -m pytest -q
# If you only have the `python` launcher:
#   python -m pip install -r requirements-dev.txt
#   python -m pytest -q
#
# Tests use the same "%b %Y" month strings as the app (e.g. "Jan 2024"). Those
# require an English month abbreviation locale (typical on en-US Windows).
-r requirements.txt
pytest>=7.4.0
pytest-mock>=3.12.0
